Функция toupper() конвертирует символ из нижнего регистра в верхний.
Если  аргумент это символ в нижнем регистре - возвращается его аналог в верхнем регистре, если представление символа существует в текущей локали.
Иначе возвращается сам аргумент.

#include <ctype.h>
int toupper(int c);
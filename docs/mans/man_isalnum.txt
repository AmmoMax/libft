Функция isalnum() возвращает ненулевое значение если аргумент является либо буквой алфавита (верхнего или нижнего регистра) либо цифрой.
Если символ не буква и не цифра - возвращается 0.

#include <ctype.h>
int	isalnum(int c);
Функция memcpy копирует область памяти.
#include <string.h>

void *memcpy(void *dest, const void *src, size_t n);

Функция копирует n байт из памяти из области src в память в область dest. Области памяти не должны перекрываться.

Возвращает указатель на новую область памяти.

